import { MapKeys } from "../helpers";

const defaultStore = {
  historyDisplay: "",
  history: [],
  displayValue: "0",
  keyDown: "",
  muted: false,
  calculated: false,
  keys: MapKeys,
};

export default defaultStore;
